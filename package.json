{
  "name": "axiom",
  "private": true,
  "packageManager": "npm@10.2.4",
  "scripts": {
    "dev": "turbo run dev",
    "build": "turbo run build",
    "test": "turbo run test",
    "lint": "turbo run lint"
  },
  "workspaces": [
    "apps/*",
    "packages/*",
    "demo-engine"
  ],
  "devDependencies": {
    "turbo": "latest"
  }
}
